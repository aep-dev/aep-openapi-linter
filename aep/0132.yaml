rules:
  aep-132-http-body:
    description: A list operation must not accept a request body.
    severity: error
    formats: ['oas3']
    given:
      # Only run this rule on paths that do not end in a path param
      - $.paths[?(!@path.match(/\}']$/))].get.requestBody
    then:
      function: falsy
